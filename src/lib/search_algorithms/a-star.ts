import { TileType } from "../../types/settings";

const h = () => {};

export const aStarAlgorithm = (
  grid: TileType[][],
  updateTile: (
    row: number,
    column: number,
    newTileType: TileType,
    foreUpdate?: boolean
  ) => void
) => {};
